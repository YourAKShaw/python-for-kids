# accept a value for `a` as an integer from the user
a = int(input("a = "))

# accept a value for `b` as an integer from the user
b = int(input("b = "))

sum = a + b

# print the sum to the console
print("Sum of a and b is", sum)